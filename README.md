# 🤖 Telegram Giveaway Aggregator

Бот для автоматического сбора, фильтрации и пересылки розыгрышей из Telegram-каналов.
Поддерживает мульти-аккаунты для обхода лимитов и Docker для быстрого запуска.

---

## 🚀 Быстрый старт (Инструкция для новичков)

Эта инструкция поможет вам запустить бота на арендованном сервере (VPS/VDS) с нуля. Вам не нужно ничего скачивать на свой компьютер.

### Шаг 1: Подготовка Telegram аккаунтов (ОБЯЗАТЕЛЬНО)

Перед тем как арендовать сервер, подготовьте аккаунты:

1.  **Главный аккаунт (Юзербот №1):**
    *   Зайдите в бота **[@trustat](https://t.me/trustat)** и настройте "Мониторинг" по ключевым словам (например: `розыгрыш`, `дарим`).
    *   Обязательно запустите бота уведомлений **[@TrustatAlertsBot](https://t.me/TrustatAlertsBot)** (`/start`). Бот будет читать сообщения именно от него.
    *   Создайте **Целевой канал**, куда будут падать розыгрыши.
    *   Добавьте этот аккаунт в **Администраторы** вашего целевого канала с правом "Публикация сообщений".

2.  **Дополнительные аккаунты (Рабочие):**
    *   Желательно иметь 1-3 дополнительных аккаунта (можно свежереги) для "черновой работы" (вступление в каналы, переход по ссылкам).
    *   Это убережет главный аккаунт от бана.

### Шаг 2: Подключение к серверу

1.  Зайдите на свой сервер через терминал (PuTTY, Termius или консоль хостинга):
    ```bash
    ssh root@ip-вашего-сервера
    ```
2.  Обновите список программ и установите Git:
    ```bash
    apt update && apt install git -y
    ```

### Шаг 3: Установка бота

1.  Скачайте код бота:
    ```bash
    git clone https://github.com/karamans/drawing_of_prizes.git bot
    ```
    *(Замените ссылку на вашу реальную ссылку на GitHub)*

2.  Зайдите в папку:
    ```bash
    cd bot
    ```

3.  Настройте конфигурацию:
    ```bash
    cp env.example .env
    nano .env
    ```
    *   `API_ID` и `API_HASH`:
        1.  Перейдите на сайт [my.telegram.org](https://my.telegram.org) и войдите по номеру телефона.
        2.  Нажмите на ссылку **API development tools**.
        3.  В поле **App title** напишите любое название (например, `MyBot`).
        4.  В поле **Short name** напишите короткое имя (например, `mybot`).
        5.  Поле **Platform** оставьте `Desktop`.
        6.  Нажмите **Create application**.
        7.  Скопируйте значения `App api_id` и `App api_hash` и вставьте их в файл `.env`.
    *   `SESSION_NAME`: Оставьте как есть или напишите имя сессии главного аккаунта.
    *   `SOURCE_CHAT`: Если используете бота уведомлений, напишите `TrustatAlertsBot`.
    *   `TARGET_CHANNEL_ID`: Напишите юзернейм вашего канала, например `@my_giveaway_channel`.
    *   Нажмите `Ctrl+X`, затем `Y` и `Enter`, чтобы сохранить.

### Шаг 4: Установка Docker

Запустите скрипт развертывания. Если Docker не установлен, скрипт сообщит об этом.
Для быстрой установки Docker на Ubuntu/Debian выполните команду:

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

После установки проверьте, что Docker работает:
```bash
docker --version
```

Теперь запустите скрипт настройки:

```bash
chmod +x deploy.sh
./deploy.sh
```

### Шаг 5: Авторизация аккаунтов

Теперь нужно войти в Telegram-аккаунты.

1.  Запустите менеджер аккаунтов:
    ```bash
    ./deploy.sh auth
    ```
2.  Следуйте инструкциям:
    *   Сначала войдите в **Главный аккаунт** (который админ канала и подписан на Trustat).
    *   Введите номер телефона и код.
    *   Скрипт спросит "Do you want to add a NEW account?". Нажмите `y` и войдите в дополнительные аккаунты.

### Шаг 6: Запуск

Когда аккаунты добавлены, запустите бота:

```bash
./deploy.sh
```

**Готово!** Бот работает в фоновом режиме.

---

## 🛠 Управление

*   **Посмотреть логи (что делает бот):**
    ```bash
    ./deploy.sh logs
    ```
    *(Нажмите `Ctrl+C` чтобы выйти из логов)*

*   **Добавить новые аккаунты:**
    ```bash
    ./deploy.sh auth
    ```
    *(После добавления нужно перезапустить бота)*

*   **Перезапустить бота:**
    ```bash
    ./deploy.sh stop
    ./deploy.sh
    ```

## ❓ Решение проблем

*   **Ошибка `[400 PEER_ID_INVALID]`**: Бот не может найти целевой канал.
    *   Решение: Убедитесь, что в `.env` вы указали верный `@username` канала, и что Главный аккаунт является его Администратором.
*   **Ошибка `[420 FLOOD_WAIT_X]`**: Telegram попросил подождать X секунд (слишком частые запросы).
    *   Решение: Ничего делать не нужно. Бот сам уснет на это время и продолжит работу. Не перезапускайте его, это только сбросит таймер.
*   **Бот шлет только текст**:
    *   Решение: Скорее всего, это приватный канал. Бот копирует текст и ссылку, так как не может сделать репост (Forward) из канала, где Главный бот не состоит.
